# OAuth credentials and tokens written by app/server.js — never commit these
.data/
.env

node_modules/
*.log
.DS_Store

# Built executables — far too large for git; build them with app/build-exe.mjs
build/

# Bundled local defaults may hold an API key — never commit it
app/defaults.json
dist/twin-seo-source.zip
